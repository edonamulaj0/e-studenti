{
	"name": "r2-catalog-manager",
	"version": "1.0.0",
	"description": "Cloudflare Worker for managing R2 catalog",
	"main": "src/index.js",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"fix-current-contributors": "wrangler d1 execute e-studenti-auth --remote --file=./fix-current-contributors.sql"
	},
	"dependencies": {
		"fflate": "^0.8.1"
	},
	"devDependencies": {
		"wrangler": "^3.0.0"
	}
}